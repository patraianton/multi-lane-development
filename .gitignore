node_modules/
*.log
state/

# Lavish worker: generated assets and deploy config stay out of git
deploy/lavish-worker/assets.gen.mjs
deploy/lavish-worker/wrangler.gen.jsonc
node_modules/

# Local sidecar (briefs, design drafts, server logs) kept next to the checkout, never part of the project
.local/

# Local project material next to the public repo (owner context digests, grounding reports, copies of ops docs)
context/
reports/
